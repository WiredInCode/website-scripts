# website-scripts

Collection of scripts &amp; resources associated with WiredInCode.com articles. 

| Scripts                          | Description                                                                                                                                                                               |
|:-------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2FA_QR_decoder.html              | Tool to decode 2FA TOTP QR codes to retrieve the secret key.  <br/> The QR processing is Javascript based, so everything is done in your local browser and the secrets are never exposed. |
| convert_html_images_to_base64.py | Process an .html file and convert all images (`<img>`) to inlined base64 to allow for easy one-file sharing and offline viewing.                                                          |
|                                  |                                                                                                                                                                                           |
|                                  |                                                                                                                                                                                           |
|                                  |                                                                                                                                                                                           |

Tool to decode 2FA TOTP QR codes to retrieve the secret key.    
 The QR processing is Javascript based, so everything is done in your local browser and the secrets are never exposed.

- 2FA_QR_decoder.html
  
  - Tool to decode 2FA TOTP QR codes to retrieve the secret key.
  
  -  The QR processing is Javascript based, so everything is done in your local browser and the secrets are never exposed.
     

- convert_html_images_to_base64.py
  
  - Process an .html file and convert all images (`<img>`) to inlined base64 to allow for easy one-file sharing and offline viewing. 
